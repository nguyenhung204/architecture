spring.application.name=be
server.port=3001

# DevTools Configuration for Hot Reload
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
spring.devtools.restart.additional-paths=src/main/java
spring.devtools.restart.exclude=static/**,public/**,templates/**

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/test

# Jackson Configuration for Date/Time serialization
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC


# Redis Configuration
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.database=${REDIS_DATABASE:0}
spring.data.redis.timeout=${REDIS_TIMEOUT:2000ms}

# JWT Configuration
jwt.secret=your-super-secret-key-that-should-be-256-bits-long-and-very-secure
jwt.access-token.expiration=900000
jwt.refresh-token.expiration=604800000

# Email Configuration (Gmail example)
# Email Configuration (Gmail example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nguyenhung2004200@gmail.com
spring.mail.password=bhinhftpxbuyufca
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.debug=true

# Disable mail health check temporarily
management.health.mail.enabled=false

# OTP Configuration
otp.expiration=120000
otp.length=6

# Cookie Configuration
cookie.domain=localhost
cookie.secure=false
cookie.same-site=none
